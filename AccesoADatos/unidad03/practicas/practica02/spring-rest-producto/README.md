# Servicio Red en Spring

Accedemos a la api mediante el enlace:

```
http://localhost:8080/swagger-ui.html
```

> [!NOTE]
> No se puede acceder de forma normal, hay que añadir *Swagger* para poder acceder.